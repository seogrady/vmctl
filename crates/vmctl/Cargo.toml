[package]
name = "vmctl"
version = "0.1.0"
edition.workspace = true

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
handlebars = "5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

